apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-data
  namespace: cloudlab
spec:
  # 不写死存储类：走集群默认（minikube 为 standard，k3s 为 local-path）
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
